# Slack workspace export

Drop your unzipped **Slack export** here (the folder with `users.json`,
`channels.json`, and per-channel message folders) or pass a `.zip`.
Contents are git-ignored. Members → people, channels → project notes, and
messages → **signal only** (per-person frequency/recency — bodies are NEVER read).
