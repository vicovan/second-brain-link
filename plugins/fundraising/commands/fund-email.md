---
description: Draft investor emails, intro requests or follow-ups — never sent, always drafts
---

Invoke the `raise-outreach` skill for: $ARGUMENTS

Nothing is sent. When one has actually been sent, say so and the record is marked with `ledger.py sent <key>`.
