---
description: Set up (or update) your fundraising profile — facts, round, filter chain, autonomy
---

Invoke the `raise-onboarding` skill.

If a profile already exists, show what would change before writing anything.
